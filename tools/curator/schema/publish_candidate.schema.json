{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://harshithkantamneni.github.io/schemas/publish_candidate.schema.json",
  "title": "Publish Candidate",
  "description": "A lab artifact eligible for publication on the portfolio site. Each lab appends one of these to its publish_candidates/ directory when an artifact ratifies.",
  "type": "object",
  "required": [
    "id",
    "lab",
    "type",
    "status",
    "title",
    "summary",
    "source_artifacts",
    "ratified_at",
    "curator_state"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$",
      "minLength": 3,
      "maxLength": 80,
      "description": "URL-safe slug. Used as the page filename and the unique identifier across the manifest."
    },
    "lab": {
      "type": "string",
      "enum": ["hive", "agi"],
      "description": "Source lab. The curator scopes its run to one lab per invocation to avoid cross-lab content bleed."
    },
    "type": {
      "type": "string",
      "enum": ["report", "note"],
      "description": "Long-form report (~2,500-4,000 words) or short-form note (~500-1,500 words)."
    },
    "status": {
      "type": "string",
      "enum": ["draft", "ready", "published", "held", "superseded"],
      "description": "Lab-side ratification status. The curator only processes 'ready'."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Public title for the published page."
    },
    "summary": {
      "type": "string",
      "minLength": 20,
      "maxLength": 500,
      "description": "One-to-two sentence summary used in writing list, RSS feed, and social cards."
    },
    "source_artifacts": {
      "type": "array",
      "items": {
        "oneOf": [
          {
            "type": "string",
            "minLength": 1,
            "description": "Lab-relative path. Resolves against the entry's own lab root."
          },
          {
            "type": "object",
            "required": ["lab", "path"],
            "additionalProperties": false,
            "properties": {
              "lab": {"type": "string", "enum": ["hive", "agi"]},
              "path": {"type": "string", "minLength": 1}
            },
            "description": "Cross-lab source: lab-prefixed path. Resolves against that named lab's root."
          }
        ]
      },
      "minItems": 1,
      "description": "Source files the curator reads when adapting. Plain strings resolve against the entry's own lab root (~/Desktop/Fun/lab/ for hive, ~/Desktop/AGI/ for agi). Object form {lab, path} is for cross-lab sources (e.g. a HIVE-authored piece that observes AGI source material)."
    },
    "ratified_at": {
      "type": "string",
      "minLength": 1,
      "description": "Cycle ID (e.g. 'C108') or named ratification anchor (e.g. 'closure_memo_2026-04-18'). The lab's own discipline determines the value."
    },
    "ratified_date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "ISO date when the artifact was ratified by the lab."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "uniqueItems": true,
      "description": "Topic tags. Used for cross-piece linking and future tag-based browsing."
    },
    "channels": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["website", "hackernews", "linkedin", "buttondown"]
      },
      "uniqueItems": true,
      "default": ["website"],
      "description": "Channels to cross-post to. Website is always included; HN/LinkedIn/Buttondown are opt-in per-piece. HN + LinkedIn produce paste-ready files for manual submission (no auto-post). Buttondown creates a draft email via API (status=draft); the operator reviews + sends in Buttondown's UI."
    },
    "voice_fit_target": {
      "type": "number",
      "minimum": 0,
      "maximum": 10,
      "description": "Minimum voice-fit score required to pass the voice judge. Default 6.5 per Task 1 calibration."
    },
    "novelty_target": {
      "type": "number",
      "minimum": 0,
      "maximum": 10,
      "description": "Minimum novelty score required (10 = entirely new vs. already-published)."
    },
    "risk_tier": {
      "anyOf": [
        {"type": "integer", "minimum": 1, "maximum": 3},
        {"type": "null"}
      ],
      "description": "Risk tier (1=PR required, 2=auto-merge if judges pass, 3=24h veto window). Null until classifier runs."
    },
    "curator_state": {
      "type": "string",
      "enum": ["pending", "processing", "published", "held", "superseded"],
      "description": "Curator-side processing state. Lab writes 'pending'. Curator transitions through processing, then to one of the terminal states."
    },
    "published_url": {
      "type": "string",
      "format": "uri",
      "description": "Set by curator on successful publish."
    },
    "published_at": {
      "type": "string",
      "format": "date-time",
      "description": "Set by curator on successful publish."
    },
    "held_reason": {
      "type": "string",
      "description": "Set by curator if status transitions to 'held'. Names the failing gate (e.g. 'voice judge: 5.2/10 below threshold 6.5')."
    },
    "scores": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "voice_fit": {"type": "number"},
        "factcheck_pass": {"type": "boolean"},
        "factcheck_unverified": {"type": "array", "items": {"type": "string"}},
        "novelty": {"type": "number"}
      },
      "description": "Set by curator after judges run. Persisted in the manifest entry as audit trail."
    }
  }
}
