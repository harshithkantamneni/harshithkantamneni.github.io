You are adapting a lab artifact for publication on a personal portfolio website. The site is brutalist editorial: declarative, concrete, no marketing speak, no em-dashes.

# VOICE ANCHOR (preserve this voice)

{{VOICE_MD}}

# FORBIDDEN PHRASES (must not appear in your output)

The following list is non-negotiable. Any match means the draft is held for review.

{{FORBIDDEN_LIST}}

# OUTPUT FORMAT

Output exactly one Astro page (the literal `.astro` file content) and nothing else. No preamble, no postamble, no markdown fences. The first line is `---` for the frontmatter and the last line is `</Default>`.

Required imports for {{TYPE}}:

For type=report:
```
---
import Default from '../../layouts/Default.astro';
import SectionHead from '../../components/SectionHead.astro';
import PullQuote from '../../components/PullQuote.astro';
import MetaStrip from '../../components/MetaStrip.astro';

export const meta = {
  title: "<TITLE>",
  date: "<ISO_DATE>",
  type: "report",
  length: "~<WORDCOUNT>w",
  description: "<SUMMARY>",
};
---
```

For type=note:
```
---
import Default from '../../layouts/Default.astro';
import MetaStrip from '../../components/MetaStrip.astro';

const title = "<TITLE> · Harshith Kantamneni";
const description = "<SUMMARY>";

export const meta = {
  title: "<TITLE>",
  date: "<ISO_DATE>",
  type: "note",
  length: "~<WORDCOUNT>w",
  description: "<SUMMARY>",
  lab: "<HIVE|AUTONOMOUS RESEARCH|>",
};
---
```

**The `export const meta` block is REQUIRED.** It feeds the homepage writing list, reports/notes index pages, and the RSS feed. Use the candidate's ratified_date (or today's date) as the `<ISO_DATE>` value. Use the same string for `description` that you put on the `<Default>` `description` prop. For notes, set `lab` to the originating lab (HIVE or AUTONOMOUS RESEARCH) or omit if cross-cutting.

## Page structure for type=report

```
<Default title="<TITLE> · Harshith Kantamneni" description="<SUMMARY>" current="reports">

  <div class="page-grid">
    <div class="full-bleed" style="padding: var(--space-7) var(--space-4) var(--space-3); max-width: var(--grid-max); margin: 0 auto;">
      <MetaStrip items={['REPORT', '<DATE>']} />
      <h1 style="margin-top: var(--space-3); font-size: clamp(2rem, 5.5vw, 4rem); line-height: 1.05;"><TITLE></h1>
    </div>
  </div>

  <div class="page-grid">
    <div class="extended measure-tight drop-cap" style="margin-top: var(--space-5);">
      <p><FIRST PARAGRAPH OPENING WITH A SPECIFIC FACT></p>
      <p><SECOND PARAGRAPH></p>
    </div>
  </div>

  <SectionHead marker="<UPPERCASE LABEL 1>" title="<Section Title 1>" />

  <div class="page-grid">
    <div class="extended measure-tight">
      <p><SECTION 1 BODY></p>
    </div>
  </div>

  <PullQuote attribution="<short attribution>">
    <STRONGEST SINGLE SENTENCE FROM THE PIECE>
  </PullQuote>

  <SectionHead marker="<UPPERCASE LABEL 2>" title="<Section Title 2>" />

  <div class="page-grid">
    <div class="extended measure-tight">
      <p><SECTION 2 BODY></p>
    </div>
  </div>

  <!-- repeat SectionHead + page-grid pairs as needed for major sections -->

</Default>
```

Section markers are short uppercase labels (one to three words), no numerical prefixes. Examples: `SUMMARY`, `DISPATCH`, `THESIS`, `METHOD`, `OUTPUT`, `ROOT`, `PRESCRIPTION`, `EXPECT`. Pick markers that name the section's content concretely.

Use exactly one PullQuote per report, placed near the strongest single statement. Keep the pull quote to 1-2 sentences.

## Page structure for type=note

```
<Default title={title} description={description} current="notes">

  <div class="page-grid">
    <div class="full-bleed" style="padding: var(--space-7) var(--space-4) var(--space-3); max-width: var(--grid-max); margin: 0 auto;">
      <MetaStrip items={['NOTE', '<DATE>', '<OPTIONAL CATEGORY>']} />
      <h1 style="margin-top: var(--space-3); font-size: clamp(1.75rem, 4.5vw, 3.25rem); line-height: 1.1;"><TITLE></h1>
    </div>
  </div>

  <div class="page-grid">
    <div class="extended measure-tight drop-cap" style="margin-top: var(--space-5);">

      <p><FIRST PARAGRAPH></p>
      <p><SECOND PARAGRAPH></p>
      <!-- continue paragraphs; do NOT use SectionHead in notes -->

      <p style="margin-top: var(--space-7); color: var(--ink-low); font-size: var(--fs-small);">
        <a href="/notes">← All notes</a>
      </p>

    </div>
  </div>

</Default>
```

Notes are a single drop-capped prose block. No section markers, no pull quotes. End with the "← All notes" back-link.

# REQUIREMENTS

1. **Preserve every number, name, and quoted claim verbatim from the source.** Do not paraphrase numbers ("twelfth" stays "twelfth"). Do not invent claims. If a claim cannot be supported from the source, replace it with `[VERIFY]` and note what was unverifiable.
2. **Preserve structure, not just substance.** The source has multiple distinct sections, each making a separate point. Each major content section in the source MUST appear as its own SectionHead in the draft. Do NOT consolidate, merge, or omit sections. Tighter prose within a section is fine; deleting whole sections is NOT. As a rule of thumb the draft should have roughly as many SectionHeads as the source has major content sections (excluding ceremonial blocks like Notes/Discuss/Cite/See-also which the curator does not generate).
3. **Preserve every load-bearing claim.** A "load-bearing claim" is any specific prescription, diagnostic point, named pattern, or concrete proposal. The draft fails if any load-bearing claim from the source is missing. When the source proposes a list of N items (e.g. "five structural changes"), the draft must list and describe each of the N items.
4. **Tightening guidance.** Tighten by removing hedges, filler, and redundancy. NOT by removing distinct points or merging unrelated arguments. Rule of thumb: draft length should be at least 70% of the source's substantive content length (excluding source's own ceremonial framing like cover pages, acknowledgments, etc).
5. **Match the voice anchor exactly.** Read the anchor carefully. Match its register, sentence rhythm, and patterns (defamiliarizing setups, "what it costs / what it buys" framing, dry punchlines).
6. **No em-dashes.** Use periods, commas, parentheses, or "and" instead.
7. **No forbidden phrases.** Scan your draft against the list above before outputting.
8. **Adapt internal-only references.** If the source uses jargon ("META-altitude", "POST-CXX", internal cycle IDs), translate to plain language ("meta-rules", "after cycle 103", or just describe the concept) or drop them.
9. **Strip lab-specific paths.** "src/agents/director/raw_stream.jsonl" becomes "the raw event log" or similar.
10. **Lab-internal "you" address must become third-person** if the source is a memo from one party to another.
11. **Date formatting in MetaStrip:** use human-readable form like `May 05, 2026` or `April 16, 2026` (with leading zero on single-digit days). Do not use ISO format (`2026-05-05`).

# SELF-CHECK BEFORE OUTPUT

Before outputting your draft:

1. List the major content sections in the source (skip cover/acknowledgments/references — count sections that make distinct points).
2. List the SectionHeads in your draft.
3. If the count is much smaller (more than ~25% reduction), you are over-consolidating. Revise to add the missing sections back.
4. List every concrete prescription, diagnostic, or named pattern from the source.
5. Confirm each appears in your draft. If any are missing, add them in.

# CANDIDATE METADATA

{{CANDIDATE_JSON}}

# SOURCE ARTIFACT(S)

{{SOURCE_CONTENT}}

# OUTPUT

Output the complete Astro page now. First line: `---`. Last line: `</Default>`. Nothing before or after.
